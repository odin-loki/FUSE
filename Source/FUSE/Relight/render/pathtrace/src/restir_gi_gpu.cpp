// FUSE Relight RL-5.3: ReSTIR GI on the GPU (see restir_gi_gpu.hpp).
#include <fuse/relight/render/pathtrace/restir_gi_gpu.hpp>

#include <fuse/renderer/vk/allocator.hpp>
#include <fuse/renderer/vk/device.hpp>

#include <algorithm>
#include <cstring>

#if defined(FUSE_VULKAN_BACKEND)
#include <vulkan/vulkan.h>

// Generated by render/pathtrace/restir_gi_embed.cmake: kRlRestirGi{Slang,Glsl}Spv, FUSE_RL_RESTIR_GI_{SLANG,GLSL}.
#include "rl_restir_gi_spv.h"
#endif

namespace fuse::relight::render::pathtrace {

namespace rr = fuse::renderer;
namespace rg = fuse::renderer::rg;

namespace {
constexpr u32 kGroup = 8u;
constexpr u64 kWordBytes = 16u;
constexpr u64 kSlotStride = 256u; ///< ring slot: 4 addresses + kRgiParamWords float4 (64 bytes)

u64 align256(u64 v) { return (v + 255u) & ~u64{255u}; }

u32 storageUsage() {
    return static_cast<u32>(rr::BufferUsage::Storage) | static_cast<u32>(rr::BufferUsage::ShaderDeviceAddress) |
           static_cast<u32>(rr::BufferUsage::TransferSrc);
}

constexpr const char* kSectionNames[kRgiSections] = {
    "relight.restir_gi.surfaces0", "relight.restir_gi.surfaces1", "relight.restir_gi.initial",
    "relight.restir_gi.a",         "relight.restir_gi.b",         "relight.restir_gi.history",
    "relight.restir_gi.output"};

enum StageId : u32 {
    kStageSurface = 1u,
    kStageInitial = 2u,
    kStageTemporal = 3u,
    kStageShade = 4u,
    kStageSpatial = 8u,
};
} // namespace

RestirGiGpu::RestirGiGpu() = default;
RestirGiGpu::~RestirGiGpu() { destroy(); }

bool RestirGiGpu::init(const RestirGiGpuDesc& desc) {
    destroy();
    m_desc = desc;
#if defined(FUSE_VULKAN_BACKEND)
    if (desc.device == nullptr || desc.allocator == nullptr || desc.bindless == nullptr || desc.framesInFlight == 0u ||
        desc.framesInFlight > kMaxSlots) {
        m_reason = "incomplete RestirGiGpuDesc";
        return false;
    }
    m_retired.reserve(32);
    m_initialized = true;
    if (!createPipeline()) {
        m_reason = "no ReSTIR GI kernel of the requested language / pipeline creation failed";
        destroy();
        return false;
    }
    m_reason = "ok";
    return true;
#else
    m_reason = "stub backend";
    return false;
#endif
}

void RestirGiGpu::destroy() {
    if (!m_initialized) {
        return;
    }
#if defined(FUSE_VULKAN_BACKEND)
    collectRetired(~0ull);
    for (rr::Buffer& b : m_sections) {
        if (b.handle != nullptr) {
            m_desc.allocator->destroyBuffer(b);
        }
        b = rr::Buffer{};
    }
    if (m_ring.handle != nullptr) {
        m_desc.allocator->destroyBuffer(m_ring);
    }
    m_ring = rr::Buffer{};
    const VkDevice device = static_cast<VkDevice>(m_desc.device->nativeHandle());
    if (m_pipeline != nullptr) {
        vkDestroyPipeline(device, static_cast<VkPipeline>(m_pipeline), nullptr);
    }
    if (m_layoutHandle != nullptr) {
        vkDestroyPipelineLayout(device, static_cast<VkPipelineLayout>(m_layoutHandle), nullptr);
    }
#endif
    m_pipeline = nullptr;
    m_layoutHandle = nullptr;
    m_retired.clear();
    for (u64& b : m_sectionBytes) {
        b = 0;
    }
    m_width = 0;
    m_height = 0;
    m_historyValid = false;
    m_frameReady = false;
    m_language = "none";
    m_stats = RestirGiGpuStats{};
    m_initialized = false;
}

bool RestirGiGpu::createPipeline() {
#if defined(FUSE_VULKAN_BACKEND)
    const u32* words = nullptr;
    usize bytes = 0;
#if defined(FUSE_RL_RESTIR_GI_SLANG)
    if (m_desc.language == PtKernelLanguage::Auto || m_desc.language == PtKernelLanguage::Slang) {
        words = kRlRestirGiSlangSpv;
        bytes = sizeof(kRlRestirGiSlangSpv);
        m_language = "slang";
    }
#endif
#if defined(FUSE_RL_RESTIR_GI_GLSL)
    if (words == nullptr && (m_desc.language == PtKernelLanguage::Auto || m_desc.language == PtKernelLanguage::Glsl)) {
        words = kRlRestirGiGlslSpv;
        bytes = sizeof(kRlRestirGiGlslSpv);
        m_language = "glsl";
    }
#endif
    if (words == nullptr) {
        return false;
    }
    const VkDevice device = static_cast<VkDevice>(m_desc.device->nativeHandle());
    VkDescriptorSetLayout setLayout = static_cast<VkDescriptorSetLayout>(m_desc.bindless->layoutHandle());
    VkPushConstantRange range{VK_SHADER_STAGE_COMPUTE_BIT, 0, static_cast<u32>(sizeof(Push))};
    VkPipelineLayoutCreateInfo layoutInfo{};
    layoutInfo.sType = VK_STRUCTURE_TYPE_PIPELINE_LAYOUT_CREATE_INFO;
    layoutInfo.setLayoutCount = 1u;
    layoutInfo.pSetLayouts = &setLayout;
    layoutInfo.pushConstantRangeCount = 1;
    layoutInfo.pPushConstantRanges = &range;
    VkPipelineLayout layout = VK_NULL_HANDLE;
    if (vkCreatePipelineLayout(device, &layoutInfo, nullptr, &layout) != VK_SUCCESS) {
        return false;
    }
    m_layoutHandle = layout;
    VkShaderModuleCreateInfo moduleInfo{};
    moduleInfo.sType = VK_STRUCTURE_TYPE_SHADER_MODULE_CREATE_INFO;
    moduleInfo.codeSize = bytes;
    moduleInfo.pCode = words;
    VkShaderModule module = VK_NULL_HANDLE;
    if (vkCreateShaderModule(device, &moduleInfo, nullptr, &module) != VK_SUCCESS) {
        return false;
    }
    VkComputePipelineCreateInfo info{};
    info.sType = VK_STRUCTURE_TYPE_COMPUTE_PIPELINE_CREATE_INFO;
    info.flags = static_cast<VkPipelineCreateFlags>(m_desc.bindless->pipelineCreateFlags());
    info.stage.sType = VK_STRUCTURE_TYPE_PIPELINE_SHADER_STAGE_CREATE_INFO;
    info.stage.stage = VK_SHADER_STAGE_COMPUTE_BIT;
    info.stage.module = module;
    info.stage.pName = "main";
    info.layout = layout;
    VkPipeline pipeline = VK_NULL_HANDLE;
    const VkResult result = vkCreateComputePipelines(device, VK_NULL_HANDLE, 1, &info, nullptr, &pipeline);
    vkDestroyShaderModule(device, module, nullptr);
    m_pipeline = pipeline;
    return result == VK_SUCCESS;
#else
    return false;
#endif
}

bool RestirGiGpu::ensureBuffer(rr::Buffer& buffer, u64 bytes, u32 memory, const char* name, u8* queue) {
    if (buffer.handle != nullptr && bytes <= buffer.desc.size) {
        return true;
    }
    rr::BufferDesc d{};
    d.size = static_cast<usize>(bytes);
    d.usage = static_cast<rr::BufferUsage>(storageUsage());
    d.memoryUsage = static_cast<rr::MemoryUsage>(memory);
    d.name = name;
    rr::Buffer fresh{};
    if (!m_desc.allocator->createBuffer(d, fresh) || fresh.mapped == nullptr || fresh.deviceAddress == 0u) {
        if (fresh.handle != nullptr) {
            m_desc.allocator->destroyBuffer(fresh);
        }
        return false;
    }
    if (buffer.handle != nullptr) {
        m_retired.push_back(Retired{buffer, m_frameSerial});
        ++m_stats.reallocations;
    }
    buffer = fresh;
    if (queue != nullptr) {
        *queue = rg::kNoQueue;
    }
    return true;
}

bool RestirGiGpu::beginFrame(u64 frameSerial, const PtCompiledScene& scene, const PtFrameDesc& frame,
                             const RestirGiSettings& settings) {
    m_frameReady = false;
    if (!m_initialized || !scene.valid() || frame.width == 0u || frame.height == 0u) {
        return false;
    }
    m_frameSerial = frameSerial;
    m_settings = settings;
    m_stats.passes = 0;
    const bool resized = frame.width != m_width || frame.height != m_height;
    m_width = frame.width;
    m_height = frame.height;
    // Persistent sections.
    const u64 pixels = u64(frame.width) * frame.height;
    u64 want[kRgiSections];
    want[kRgiSecSurface0] = pixels * kRgiSurfaceWords * kWordBytes;
    want[kRgiSecSurface1] = want[kRgiSecSurface0];
    for (u32 s = kRgiSecInitial; s <= kRgiSecHistory; ++s) {
        want[s] = pixels * kRgiReservoirWords * kWordBytes;
    }
    want[kRgiSecOutput] = pixels * kRgiOutWords * kWordBytes;
    bool reallocated = false;
    for (u32 s = 0; s < kRgiSections; ++s) {
        const bool had = m_sections[s].handle != nullptr;
        const u64 before = had ? m_sections[s].desc.size : 0u;
        if (!ensureBuffer(m_sections[s], align256(want[s]), static_cast<u32>(rr::MemoryUsage::GpuToCpu),
                          kSectionNames[s], &m_sectionQueues[s])) {
            m_reason = "ReSTIR GI section buffer";
            return false;
        }
        reallocated = reallocated || !had || m_sections[s].desc.size != before;
        m_sectionBytes[s] = want[s];
    }
    if (resized || reallocated) {
        m_historyValid = false;
    }
    // Ring slot: the stage block.
    if (m_ring.handle == nullptr &&
        !ensureBuffer(m_ring, kSlotStride * m_desc.framesInFlight, static_cast<u32>(rr::MemoryUsage::CpuToGpu),
                      "relight.restir_gi.ring", &m_ringQueue)) {
        m_reason = "ReSTIR GI ring";
        return false;
    }
    m_blockOffset = u64(frameSerial % m_desc.framesInFlight) * kSlotStride;
    m_cur ^= 1u;
    m_flags = (settings.unbiased ? kRgiFlagUnbiased : 0u) | (m_historyValid ? kRgiFlagHistory : 0u) |
              (settings.temporal ? kRgiFlagTemporal : 0u);
    u8* slot = static_cast<u8*>(m_ring.mapped) + m_blockOffset;
    const u32 curSec = m_cur == 0u ? kRgiSecSurface0 : kRgiSecSurface1;
    const u32 prevSec = m_cur == 0u ? kRgiSecSurface1 : kRgiSecSurface0;
    const u64 block[4] = {m_sections[curSec].deviceAddress, m_sections[prevSec].deviceAddress,
                          m_sections[kRgiSecHistory].deviceAddress, m_sections[kRgiSecOutput].deviceAddress};
    std::memcpy(slot, block, sizeof(block));
    Word params[kRgiParamWords];
    packRestirGiParams(settings, m_flags, 0u, params);
    std::memcpy(slot + 32u, params, sizeof(params));
    m_frameReady = true;
    ++m_stats.frames;
    return true;
}

bool RestirGiGpu::addPasses(rg::Graph& graph, PathTracerGpu& pt, const PtGraphRefs& ptRefs) {
    if (!m_initialized || !m_frameReady || !ptRefs.valid) {
        return false;
    }
    if (!pt.addLightsPass(graph, ptRefs)) {
        m_reason = "light convert pass";
        return false;
    }
    const PtTraceBindings b = pt.traceBindings();
    if (b.width != m_width || b.height != m_height || b.params == 0u) {
        m_reason = "PathTracerGpu frame does not match";
        return false;
    }
    m_refs = RestirGiGraphRefs{};
    for (u32 s = 0; s < kRgiSections; ++s) {
        m_refs.sections[s] = graph.importBuffer(rg::ImportedBuffer{
            m_sections[s].handle, m_sections[s].desc.size, m_sectionQueues[s], &m_sectionQueues[s], kSectionNames[s]});
    }
    m_refs.ring = graph.importBuffer(
        rg::ImportedBuffer{m_ring.handle, m_ring.desc.size, m_ringQueue, &m_ringQueue, "relight.restir_gi.ring"});
    m_refs.ringRange = rg::BufferRange{m_blockOffset, 64u};
    m_refs.valid = true;

    Push base{};
    base.params = b.params;
    base.instances = b.instances;
    base.triangles = b.triangles;
    base.materials = b.materials;
    base.portals = b.portals;
    base.lightMap = b.lightMap;
    base.lightTable = ptRefs.lights.tableAddress;
    base.lightTree = ptRefs.lights.tree.header;
    base.tlas = b.tlas;
    base.lut = b.lut;
    base.block = m_ring.deviceAddress + m_blockOffset;
    base.width = m_width;
    base.height = m_height;
    base.lightCount = b.lightCount;
    const u32 curSec = currentSurfaceSection();
    const u32 prevSec = curSec == kRgiSecSurface0 ? kRgiSecSurface1 : kRgiSecSurface0;
    auto range = [&](u32 s) { return rg::BufferRange{0u, m_sectionBytes[s]}; };
    auto addr = [&](u32 s) { return m_sections[s].deviceAddress; };
    u32 count = 0;
    auto add = [&](const char* name, u32 stage, u64 src, u64 dst) -> rg::PassBuilder {
        PassRecord& r = m_records[count++];
        r = PassRecord{};
        r.self = this;
        r.push = base;
        r.push.stage = stage;
        r.push.src = src;
        r.push.dst = dst;
        r.groupsX = (m_width + kGroup - 1u) / kGroup;
        r.groupsY = (m_height + kGroup - 1u) / kGroup;
        ++m_stats.passes;
        rg::PassBuilder pass = graph.addPass(name, &RestirGiGpu::record, &r);
        pass.use(ptRefs.ring, rg::Access::StorageRead, ptRefs.ringRange, rg::kStageCompute)
            .use(m_refs.ring, rg::Access::StorageRead, m_refs.ringRange, rg::kStageCompute)
            .use(ptRefs.rt.tlas, rg::Access::AccelerationStructureRead, {}, rg::kStageCompute)
            .use(ptRefs.statics, rg::Access::StorageRead, {}, rg::kStageCompute)
            .use(ptRefs.lights.ring, rg::Access::StorageRead, ptRefs.lights.tableRange, rg::kStageCompute);
        if (ptRefs.lights.tree.tree.valid()) {
            pass.use(ptRefs.lights.tree.tree, rg::Access::StorageRead, ptRefs.lights.tree.range, rg::kStageCompute);
        }
        return pass;
    };
    auto read = [&](rg::PassBuilder& pass, u32 s) {
        pass.use(m_refs.sections[s], rg::Access::StorageRead, range(s), rg::kStageCompute);
    };
    auto write = [&](rg::PassBuilder& pass, u32 s) {
        pass.use(m_refs.sections[s], rg::Access::StorageWrite, range(s), rg::kStageCompute);
    };
    {
        rg::PassBuilder p = add("relight.restir_gi.surface", kStageSurface, 0u, addr(curSec));
        write(p, curSec);
    }
    {
        rg::PassBuilder p = add("relight.restir_gi.initial", kStageInitial, 0u, addr(kRgiSecInitial));
        read(p, curSec);
        write(p, kRgiSecInitial);
    }
    {
        rg::PassBuilder p = add("relight.restir_gi.temporal", kStageTemporal, addr(kRgiSecInitial), addr(kRgiSecA));
        read(p, curSec);
        read(p, prevSec);
        read(p, kRgiSecInitial);
        read(p, kRgiSecHistory);
        write(p, kRgiSecA);
    }
    u32 src = kRgiSecA;
    u32 dst = kRgiSecB;
    const u32 iterations = m_settings.spatialSamples > 0u ? std::min(m_settings.spatialIterations, 4u) : 0u;
    for (u32 it = 0; it < iterations; ++it) {
        rg::PassBuilder p = add("relight.restir_gi.spatial", kStageSpatial + it, addr(src), addr(dst));
        read(p, curSec);
        read(p, src);
        write(p, dst);
        std::swap(src, dst);
    }
    m_final = src;
    {
        rg::PassBuilder p = add("relight.restir_gi.shade", kStageShade, addr(src), 0u);
        read(p, curSec);
        read(p, src);
        write(p, kRgiSecOutput);
        write(p, kRgiSecHistory);
    }
    pt.setRestirGi(addr(kRgiSecOutput), m_refs.sections[kRgiSecOutput], range(kRgiSecOutput));
    m_historyValid = true;
    return true;
}

void RestirGiGpu::record(const rg::PassContext& context, void* user) {
#if defined(FUSE_VULKAN_BACKEND)
    const PassRecord& r = *static_cast<const PassRecord*>(user);
    const RestirGiGpu& self = *r.self;
    VkCommandBuffer cmd = static_cast<VkCommandBuffer>(context.commandBuffer);
    vkCmdBindPipeline(cmd, VK_PIPELINE_BIND_POINT_COMPUTE, static_cast<VkPipeline>(self.m_pipeline));
    self.m_desc.bindless->bind(cmd, VK_PIPELINE_BIND_POINT_COMPUTE, self.m_layoutHandle, 0);
    vkCmdPushConstants(cmd, static_cast<VkPipelineLayout>(self.m_layoutHandle), VK_SHADER_STAGE_COMPUTE_BIT, 0,
                       static_cast<u32>(sizeof(Push)), &r.push);
    vkCmdDispatch(cmd, r.groupsX, r.groupsY, 1u);
#else
    (void)context;
    (void)user;
#endif
}

u32 RestirGiGpu::collectRetired(u64 completedSerial) {
    u32 collected = 0;
    usize keep = 0;
    for (usize i = 0; i < m_retired.size(); ++i) {
        if (m_retired[i].serial > completedSerial) {
            m_retired[keep++] = m_retired[i];
            continue;
        }
        if (m_retired[i].buffer.handle != nullptr) {
            m_desc.allocator->destroyBuffer(m_retired[i].buffer);
        }
        ++collected;
    }
    m_retired.resize(keep);
    return collected;
}

const void* RestirGiGpu::mappedSection(u32 section) const {
    return section < kRgiSections ? m_sections[section].mapped : nullptr;
}

} // namespace fuse::relight::render::pathtrace

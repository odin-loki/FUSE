# B5.7 — Screen-Space Effects (CUDA stubs)

**Status:** SSAO / SSR / SSGI API + job wiring landed in `fuse_compute`; B5.7 follow-up adds expanded stub params, contact-harden helpers, and CPU validation tests. Full kernels and B2.6 G-buffer interop remain deferred.

**Master plan:** [FUSE_MASTER_PLAN.md](../plans/FUSE_MASTER_PLAN.md) §B5.7  
**Phase source:** [P5.md](../sources/P5.md) §5.7 (SSAO/HBAO + SSR)  
**Depends on:** B5.2 G-buffer layout, B2.6 `submit_cuda` job lane (do not rewrite B2.6/B2.7)

---

## Components

| Component | Location | Notes |
|-----------|----------|-------|
| `SSAOParams` | `Compute/include/fuse/compute/screen_space_effects.hpp` | HBAO-style SSAO params; depth/normal in, AO out; blur + contact shaping fields |
| `SSRParams` | same | Depth, normal, roughness, scene colour in; SSR out; contact-hardening fields |
| `SSGIParams` | same | SSGI stub tier — bounce budget + intensity |
| `screen_space_contact.hpp` | `Compute/include/fuse/compute/screen_space_contact.hpp` | CPU helpers: blur weight, contact AO weight, SSR contact hardening, param validation |
| `launch_ssao` / `launch_ssr` / `launch_ssgi` | `Compute/src/screen_space_effects_host.cpp` | Host launcher — CPU reference or CUDA stub kernel |
| `submit_ssao_job` / `submit_ssr_job` / `submit_ssgi_job` | `Compute/include/fuse/compute/screen_space_effects_job.hpp` | Wrap launchers in `CUDAJobDesc` → `submit_cuda` |
| `ssao.cu` / `ssr.cu` / `ssgi.cu` | `Compute/kernels/` | Placeholder `__global__` kernels when toolkit present |
| `ssao_center_sample` / `ssr_center_sample` / `ssgi_center_sample` | `Compute/src/screen_space_effects_cpu.cpp` | CPU reference center-pixel samples for unit tests |
| `ssao_blur_weight` / `ssao_contact_ao_weight` | `Compute/src/screen_space_contact.cpp` | HBAO bilateral blur + contact AO shaping (CPU reference) |
| `ssr_contact_harden_roughness` / `ssr_screen_edge_fade` | same | SSR contact hardening + screen-edge confidence fade |
| `validate_ssao_params` / `validate_ssr_params` | same | Reject invalid launch parameters before dispatch |

### Backend modes

```cpp
enum class ScreenSpaceEffectsMode : u8 { Stub, CpuReference, Cuda };
```

| Mode | When | Behaviour |
|------|------|-----------|
| **CpuReference** | `FUSE_BUILD_CUDA=ON`, no toolkit | Host reference samples + no-op launches |
| **Cuda** | Toolkit found (`FUSE_HAS_CUDA=1`) | Launches placeholder kernels via B2.6 managed stream |
| **Stub** | `FUSE_BUILD_CUDA=OFF` | `fuse_compute` not built |

Job workers call `submit_ssao_job` / `submit_ssr_job` / `submit_ssgi_job`; they never include `<cuda_runtime.h>`.

---

## Deferred pipeline slot

The render graph already reserves a CUDA pass for screen-space AO:

- `DeferredPassId::ScreenSpaceAo` — name `"screen_space_ao"` in `frame_pipeline.cpp`
- G-buffer attachments set `textureDesc.cudaInterop = true` (B5.2)

Wiring the deferred execute path to call `submit_ssao_job` / `submit_ssr_job` remains future work (B2.5 graph execution is still stub recording). Do **not** add new `DeferredPassId` values — pass count stays 16.

---

## Tests

| Target | Validates |
|--------|-----------|
| `fuse_screen_space_effects_stub` | Mode info, CPU reference samples, blur/contact helpers, param validation, direct launches, `submit_*_job` counter signal |

Run:

```bash
ctest --test-dir build --output-on-failure -R 'fuse_screen_space_effects|fuse_cuda|fuse_ray_march'
```

---

## CI / build flags

Same matrix as B2.7 — see [TRACK-B-VULKAN.md](./TRACK-B-VULKAN.md) §B2.7 backend modes.

| Configure | Result |
|-----------|--------|
| `FUSE_BUILD_CUDA=OFF` | `fuse_compute` omitted |
| `FUSE_BUILD_CUDA=ON`, toolkit missing | CPU reference + `submit_cuda` stub — CI green without `nvcc` |
| `FUSE_BUILD_CUDA=ON`, toolkit found | `FUSE_HAS_CUDA=1` — placeholder `.cu` kernels linked |

---

## Out of scope (stubs)

- Real `cudaSurfaceObject_t` from G-buffer (`import_vulkan_image` still returns `ok=false`)
- Full HBAO bilateral blur pass (CPU `ssao_blur_weight` reference only)
- Hi-Z acceleration for SSR (`use_hiz` flag only)
- Full SSR ray march + contact-harden compositing (CPU `ssr_contact_harden_roughness` reference only)
- Full SSGI multi-bounce tracing
- B2.6/B2.7 infrastructure changes

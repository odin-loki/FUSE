{
  "description": "A graph toggles an rtx.conf option layer (enabled while the counter is in [3, 6]) and blends a second one in by a Remap of the counter (strength 0..1, threshold 0.5: the bool of the blended layer applies only from strength 0.5). Layer requests are resolved at the end of each frame, so the options (and the Read components) see them one frame later.",
  "stage": "option_layer.usda",
  "mesh": "0123456789ABCDEF",
  "frames": 10,
  "dt": 0.1,
  "watch_options": ["rtx.logicFixture.flag", "rtx.logicFixture.value", "rtx.logicFixture.color"]
}
